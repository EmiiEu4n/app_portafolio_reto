import 'package:flutter/material.dart'; // Importa el paquete de Flutter para construir interfaces de usuario

// Definición de colores constantes usados en la aplicación
const primaryColor = Color(0xFFFFC107); // Color primario (un tono amarillo)
const secondaryColor = Color(0xFF242430); // Color secundario (un tono gris oscuro)
const darkColor = Color(0xFF191923); // Color oscuro (utilizado en fondos o elementos oscuros)
const bodyTextColor = Color(0xFF8B8B8D); // Color para el texto del cuerpo (un tono gris claro)
const bgColor = Color(0xFF1E1E28); // Color de fondo general de la aplicación (tono oscuro)

// Definición de otras constantes usadas en la aplicación
const defaultPadding = 20.0; // Padding por defecto en toda la aplicación
const defaultDuration = Duration(seconds: 1); // Duración por defecto usada para animaciones
const maxWidth = 1440.0; // Ancho máximo permitido para el diseño de la web
